# TODO before publishing

The README references `docs/screenshot.png`. Take a screenshot of the canvas with the demo agents populated (run `npm run dev`, open `http://localhost:5173`, capture a Cmd+Shift+4 region of the canvas at a good zoom level) and save it as `docs/screenshot.png`.

Suggested dimensions: ~1600×900 or so, PNG, dark background.

Then delete this file.
